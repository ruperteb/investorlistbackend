# Migration `20200924130022-5`

This migration has been generated by ruperteb at 9/24/2020, 3:00:22 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
PRAGMA foreign_keys=OFF;
CREATE TABLE "new_Investor" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "investorName" TEXT NOT NULL,
    "commercial" BOOLEAN NOT NULL DEFAULT false,
    "industrial" BOOLEAN NOT NULL DEFAULT false,
    "retail" BOOLEAN NOT NULL DEFAULT false,
    "residential" BOOLEAN NOT NULL DEFAULT false,
    "hotel" BOOLEAN NOT NULL DEFAULT false,
    "wc" BOOLEAN NOT NULL DEFAULT false,
    "gau" BOOLEAN NOT NULL DEFAULT false,
    "kzn" BOOLEAN NOT NULL DEFAULT false,
    "allregions" BOOLEAN NOT NULL DEFAULT false,
    "minInvest" INTEGER NOT NULL DEFAULT 0,
    "maxInvest" INTEGER NOT NULL DEFAULT 100,
    "listed" BOOLEAN NOT NULL DEFAULT false,
    "unlisted" BOOLEAN NOT NULL DEFAULT false,
    "private" BOOLEAN NOT NULL DEFAULT false,
    "bee" BOOLEAN NOT NULL DEFAULT false
);
INSERT INTO "new_Investor" ("id", "investorName", "commercial", "industrial", "retail", "hotel", "wc", "gau", "kzn", "allregions", "minInvest", "maxInvest") SELECT "id", "investorName", "commercial", "industrial", "retail", "hotel", "wc", "gau", "kzn", "allregions", "minInvest", "maxInvest" FROM "Investor";
DROP TABLE "Investor";
ALTER TABLE "new_Investor" RENAME TO "Investor";
PRAGMA foreign_key_check;
PRAGMA foreign_keys=ON
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration 20200919112309-4..20200924130022-5
--- datamodel.dml
+++ datamodel.dml
@@ -1,8 +1,8 @@
 // 1
 datasource db {
   provider = "sqlite" 
-  url = "***"
+  url = "***"
 }
 // 2
 generator client {
@@ -15,16 +15,21 @@
   investorName String
   commercial Boolean @default(false)
    industrial Boolean @default(false)
    retail Boolean @default(false)
+   residential Boolean @default(false)
    hotel Boolean @default(false)
    contacts  Contact[] 
    wc Boolean @default(false)  
    gau Boolean @default(false)
    kzn Boolean @default(false)
    allregions Boolean @default(false)
    minInvest Int @default(0)
    maxInvest Int @default(100)
+    listed Boolean  @default(false)
+   unlisted Boolean @default(false)
+   private Boolean @default(false)
+   bee Boolean @default(false)
 }
 model Contact {
    id Int      @id @default(autoincrement())
```


