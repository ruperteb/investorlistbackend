# Migration `20200912181623-initial`

This migration has been generated by ruperteb at 9/12/2020, 8:16:23 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "Investor" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "investorName" TEXT NOT NULL,
    "commercial" BOOLEAN NOT NULL,
    "industrial" BOOLEAN NOT NULL,
    "retail" BOOLEAN NOT NULL,
    "hotel" BOOLEAN NOT NULL,
    "wc" TEXT NOT NULL,
    "gau" TEXT NOT NULL,
    "kzn" TEXT NOT NULL,
    "allregions" TEXT NOT NULL
)

CREATE TABLE "Contact" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "name" TEXT NOT NULL,
    "officeNo" TEXT NOT NULL,
    "mobileNo" TEXT NOT NULL,
    "email" TEXT NOT NULL,
    "investorID" INTEGER NOT NULL,

    FOREIGN KEY ("investorID") REFERENCES "Investor"("id") ON DELETE CASCADE ON UPDATE CASCADE
)
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200912181623-initial
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,35 @@
+// 1
+datasource db {
+  provider = "sqlite" 
+  url = "***"
+}
+
+// 2
+generator client {
+  provider = "prisma-client-js"
+}
+
+// 3
+model Investor {
+  id          Int      @id @default(autoincrement())
+  investorName String
+  commercial Boolean
+   industrial Boolean
+   retail Boolean
+   hotel Boolean
+   contacts  Contact[]
+   wc String
+   gau String
+   kzn String
+   allregions String
+}
+
+model Contact {
+   id Int      @id @default(autoincrement())
+  name String
+  officeNo String
+  mobileNo String
+  email String
+  investorName Investor @relation(fields: [investorID], references: [id]) 
+  investorID Int
+}
```


